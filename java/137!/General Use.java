// Given a non-empty array of integers
// Every element appears k times except for one, which appears exactly p. 
// Find that single one.

class Solution {
    public int singleNumber(int[] nums,int k) {
            int m=(int)ceil(log2((double))k));
            
    }
}

